# Change Log: 2026-08-18 02:46:00

## Summary of Modifications

1. **Restored Missing `POST /submit_entry` Endpoint**:
   - Re-added the `@app.post("/submit_entry")` route in `backend/app/main.py` which triggers `process_entry_task`.
   - Updated QR generation in `process_entry_task` to dynamically use `RENDER_EXTERNAL_URL` in production.
