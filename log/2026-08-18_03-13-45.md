# Change Log: 2026-08-18 03:13:45

## Summary of Modifications

1. **Enhanced Session Reset & Worker Thread Lifecycle**:
   - Updated `delete_session()` in `backend/app/services/whatsapp.py` to stop the previous worker thread, disconnect client, remove all session files matching `WHATSAPP_SESSION_NAME`, and spawn a fresh connection worker thread (`self.start(force_restart=True)`).
   - Ensured that deleting the session from either the frontend modal or `/api/whatsapp/delete_session` immediately regenerates a new QR code without requiring a server reboot.
