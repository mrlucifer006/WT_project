# Change Log: 2026-08-18 02:24:30

## Summary of Modifications

1. **Fixed Neonize Event Name**:
   - Corrected `QrEv` to `QREv` in `backend/app/services/whatsapp.py`.
   - Updated event handler to extract `event.Codes[0]` from the protobuf `QREv` message.

2. **Safe Static File Mounts in FastAPI**:
   - Updated `backend/app/main.py` to only mount `/static` if the folder exists.
   - Automatically create `generated_qrs/` directory before mounting to eliminate `RuntimeError: Directory does not exist` on fresh deployments.

3. **Import Validation**:
   - Verified that `app.main` loads and initializes without errors in Python 3.
