# Change Log: 2026-08-18 03:03:30

## Summary of Modifications

1. **Delete Session Database (`session.db`) Feature**:
   - Enhanced `delete_session()` in `backend/app/services/whatsapp.py` to disconnect and purge SQLite session files (`session.db`, `my_session.sqlite3`, including WAL and SHM journal files), resetting the client state to generate a fresh QR code immediately.
   - Added `POST /api/whatsapp/delete_session` endpoint in `backend/app/main.py`.

2. **Frontend Session Deletion Controls**:
   - Added a **"Delete session.db"** action card to `frontend/data.html` with a dedicated confirmation modal and toast alerts.
   - Added a quick **"Reset session.db"** button inside the WhatsApp QR login modal on `frontend/index.html` to allow instant session reset and QR regeneration.
